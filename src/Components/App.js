import React from 'react';
import PropTypes from 'prop-types';

function App (props) {
    return (
        <div />
    );
}

App.propTypes = {
    logout: PropTypes.func,
    logoutComplete: PropTypes.bool
};

export default App;

